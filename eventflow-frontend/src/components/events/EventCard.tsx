// ============================================================
// components/events/EventCard.tsx — PHASE 2
// ============================================================
// Reusable card for displaying an event in the events list grid.
// Props: event: Event
// Shows: banner image (if set), title, status badge, venue,
//        date, entry fee, registration count
// Wraps in a Link to /events/:id

export default function EventCard() {
  return null // TODO Phase 2
}
